/**
 * Supabase-generated types go here.
 *
 * Generated in Phase 2 once the schema exists:
 *   npx supabase gen types typescript --project-id <id> > types/database.ts
 *   (or `npm run db:types` against a local Supabase)
 *
 * Until then this placeholder keeps imports resolvable.
 */
export type Database = Record<string, never>;
